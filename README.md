# Milestone5

CPP: Second set of C++ exercises. This time arguments are: containers, template, polymorphism, inheritance. The last exercise of the last module(09) consists in the implementations of the Ford-Jhonson(jacobsthal number) algorithm on two differents containers.

ft_irc : Implemented a server-client communication system using IRC protocol(v 1859) in C++.
